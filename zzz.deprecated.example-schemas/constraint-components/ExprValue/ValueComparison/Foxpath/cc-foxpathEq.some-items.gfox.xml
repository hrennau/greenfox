<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Demonstrates the use of constraint component ExprValueEq, using a Foxpath expression.
    An additional ExprValueMinCount constraint ensures that successful validation is
    not possible without evaluation of the ExprValueEq constraint.
    
    The schema checks that at least one file matching a pattern of folder and file names contains
    at a given location a given string.
    
    This example uses a quantifier (@quant) with a value 'some'. Successful validation therefore 
    only requires at least one value item to satisfy the condition.
    
    Constraint components:
    * Name:  ExprValueEq 
    - XML:   foxpath/(@expr, @eq) 
    - Check: Every item of the expression value must be equal to the value given by @eq.
    
    * Name:  ExprValueMinCount 
    - XML:   foxpath/(@expr, @minCount) 
    - Check: The number of value items must be at least the number specified by @minCount.
-->
<greenfox greenfoxURI="http://www.greenfox.org/ns/schema-examples/demo-constraint-component/foxpath-value-eq.all-items"
          xmlns="http://www.greenfox.org/ns/schema">

    <context>
        <field name="domain" value="${schemaPath}\..\..\..\..\..\..\example-data"/>
    </context>
    
    <domain path="${domain}" name="greenfox-example-data">
        <folder foxpath="airports" id="airportsFolderShape">
            <targetSize countMsg="Missing folder: airports" count="1"/>     
            <!-- All value items must satisfy the condition -->            
            <foxpath expr="*\airport-*.xml//dst"
                     eq="N"
                     quant="some"
                     eqMsg="No airport found with Daylight Savings Time None (N)."
                     minCount="60"
                     minCountMsg="Repository should contain at least 60 airport descriptions"/>
        </folder>
    </domain>

</greenfox>

