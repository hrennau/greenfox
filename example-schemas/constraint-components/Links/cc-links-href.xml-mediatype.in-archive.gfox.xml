<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Demonstrates the use of constraint components Link*, which are represented
    by a <links> element. This example checks resources contained by an archive file. 
    
    The optional @mediatype parameter requires link resolution
    to include a parsing of the link targets into a node tree.
    
    A LinksResolvable constraint (@resolvable = true) is violated if any target 
    URIs cannot be resolved. 
    
    A Link*Count constraint (@count*, @minCount*, @maxCount*) checks the number of
    - target resources (* = TargetResources)
    - target docs (* = TargetDocs)    
    - target nodes (* = TargetNodes)
    - link context nodes (* = ContextNodes)
    Target resources and target docs are counted per link context node.
    
    Demonstrates the use of constraint component LinksResolvable, which is represented
    by a <Link> element. The constraint is violated if any links produced by a given 
    expression cannot be resolved. The optional @mediatype attribute further constrains 
    the link target to have the specified mediatype (xml, json, text). The optional 
    attributes @minCount, @maxCount, @count constraint the number of items returned by 
    the expression producing the links.    
-->
<greenfox greenfoxURI="http://www.greenfox.org/ns/schema-examples/oxygen/check-catalog"
          xmlns="http://www.greenfox.org/ns/schema">

    <context>
        <field name="domain" value="${schemaPath}\..\..\..\..\example-data"/>
    </context>
  
    <domain path="${domain}" name="greenfox-example-data">
        <folder foxpath="archives\airports.zip\#archive#\airports" id="airportsFolderShape">
            <targetSize countMsg="Missing folder: airports" count="1"/>            
            <file foxpath=".\\*[//@href]" id="airportsFileShape">
                <links hrefXP="//@href" 
                    mediatype="xml"
                    resolvable="true" resolvableMsg="Link cannot be resolved"
                    minCountTargetDocs="0" minCountTargetDocsMsg="At least one target document expected"/>                
            </file>
        </folder>
    </domain>

</greenfox>
